// Local development configuration. Replace these values before deploying.
// The browser must only ever receive the Supabase anon key, never the service-role key.
window.CHOIRPORTAL_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
};
